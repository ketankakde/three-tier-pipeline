pipeline {
    agent any

    stages {
        stage('Pull') {
            steps {
                git branch: 'main', url: 'https://github.com/shubhamkalsait/EasyCRUD.git'
            }
        }

        stage('Build') {
            steps {
                sh '''
                cd backend
                cp /Dockerfile .
                docker build -t backend-app .
                '''
            }   
         }      
        stage('Deploy Backend') {
            steps {
                sh 'docker run -d -p 8081:8081 --name backend backend-app'
            }
         }
     }
}
